pragma solidity ^0.5.0;

contract SimpleAllocTest {

  struct BytePair {uint8 x; uint8 y;}

  enum Ternary {yes, no, maybeSo}

  uint8 hello;
  uint8 goodbye;
  uint potato;
  uint tomato;
  uint8 ploop;
  uint8 plop;
  uint8 plorper;
  uint8 worper;
  BytePair aPair;

  function run() public
  {
  }
}
